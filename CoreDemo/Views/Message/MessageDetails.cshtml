@model Entity.Concrete.Message2
@{
    ViewData["Title"] = "MessageDetails";
    Layout = "~/Views/Writer/WriterLayout.cshtml";
}

<h1>Mesaj Detay Sayfası</h1>
<br />
    @Html.Label("Mesaj ID")
    @Html.TextBoxFor(m =>m.Id, new {@class="form-control"})
    <br />
     @Html.Label("Mesaj Başlığı")
    @Html.TextBoxFor(m =>m.Subject, new {@class="form-control"})
    <br />
      @Html.Label("Mesaj İçeriği")
    @Html.TextAreaFor(m =>m.Details,15, 3, new {@class="form-control"})
    <br />
   
     <button class="btn btn-success">Mesajlara Dön</button>


